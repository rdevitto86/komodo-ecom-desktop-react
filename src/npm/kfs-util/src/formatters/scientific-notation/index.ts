/**
 * TODO
 */
export default class ScientificNotation {
    /**
     * 
     * @param notation 
     * @returns 
     */
    static parse(notation: string) {
        return notation;
    }

    /**
     * 
     * @param num 
     * @returns 
     */
    static convert(num: number | string) {
        return num;
    }
}
