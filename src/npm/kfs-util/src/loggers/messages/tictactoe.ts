/**
 * Working hard, or hardly work?
 */
export const TicTacToe = () => {
    // TODO - improve with random generated games, dynamic text, and highlighted wins

    console.log(`I win ;)
            \n${JSON.stringify(['X', 'O', 'X'])}
            \n${JSON.stringify([' ', 'X', ' '])}
            \n${JSON.stringify(['O', ' ', 'X'])}`);
};
