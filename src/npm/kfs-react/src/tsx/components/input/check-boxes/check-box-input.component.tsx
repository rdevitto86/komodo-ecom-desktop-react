import React from 'react';

/**
 * Records user input using check boxes
 */
export class CheckBoxInputComponent extends React.Component {
    // TODO - useState() hook + isChecked + setIsChecked
    // TODO - handle styling and container

    handleClick() {
        // TODO handle checkmark render after clicks
    }

    render() {
        return (
            <div>
                <input type="checkbox" value="Test1" />
            </div>
        );
    } 
}
